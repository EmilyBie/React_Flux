"use strict";

var React = require('react');

var HomePage = React.createClass({
  render: function() {
    return (
      <div className="jumbotron">
        <h1>Pluralsight Administration</h1>
        <p>React and React Router responsive web app</p>
      </div>
    );
  }
});

module.exports = HomePage;
